# Files tools/coverage.sh may skip, one per line, each with the reason it cannot have a spec yet.
# An entry here is a claim someone made and can be argued with. An undeclared gap is just a silence,
# which is what this file exists to prevent -- so keep it short and delete entries as they are earned.
#
# NOT exempt, deliberately, though they are currently uncovered:
#   Elmira/Core/Init.lua        332 lines of wiring -- the registrations, the load order, the pack
#                               attach. Precisely where a "looks right, does nothing" defect hides.
#   Elmira/Display/BarProviders.lua  its predecessor shipped three silent bar-glow bugs.
#
# Emptied at ADR-0014: the three entries here were all in Elmira_WoWSims/ and Elmira_Insights/,
# folders that are no longer shipped. An exemption for a file that does not exist is not harmless --
# it is a silence with nothing behind it, and it would have quietly covered a real gap if a file of
# that name ever came back.
